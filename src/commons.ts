export const CREATE_ENTITY_URL = "new";
export const DOMAIN = "http://localhost:3000";

export enum Method {
  POST = "post",
  GET = "get",
  DELETE = "delete",
}
export enum ROLE {
  USER = "user",
  ADMIN = "admin",
  GUEST = "guest",
}

export enum CRUD {
  CREATE = "CREATE",
  READ = "READ",
  UPDATE = "UPDATE",
  DELETE = "DELETE",
}

export enum ENTITIES {
  ENTITY = "entity",
  USERS = "users",
}

